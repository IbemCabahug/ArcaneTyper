# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Internal documentation lives in the private docs repository (nested clone).
# This line is the only protection against committing it here - never remove.
/docs/

# Scratch / QA artifacts
scratch/
/scratch/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent skills/rules suite (nested repo with its own .git) - never commit into the game repo.
/.samananiagents/
.samananiagents/
